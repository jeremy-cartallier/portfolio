<div class="pill"><slot /></div>

<style>
	.pill {
		display: inline-flex;
		align-items: center;
		padding: 0.4rem 0.9rem;
		gap: 0.4rem;
		color: var(--gray-100);
		border: 1px solid color-mix(in srgb, var(--accent-regular) 30%, transparent);
		background-color: color-mix(in srgb, var(--accent-regular) 12%, transparent);
		border-radius: 999rem;
		font-size: var(--text-sm);
		font-weight: 500;
		line-height: 1.35;
		white-space: nowrap;
		transition:
			background-color var(--theme-transition),
			border-color var(--theme-transition),
			transform var(--theme-transition);
	}

	.pill:hover {
		background-color: color-mix(in srgb, var(--accent-regular) 20%, transparent);
		border-color: color-mix(in srgb, var(--accent-regular) 55%, transparent);
		transform: translateY(-1px);
	}
</style>
